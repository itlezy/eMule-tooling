The hashing pipeline is correct but _RTC_CheckStackVars (debug runtime stack checks) fires on every function in the tight CryptoPP hashing loop, adding ~16x overhead. With 3 large files that's ~25 minutes at 100% CPU on one core — which looks like a
  hang when you kill it after 1-3 minutes.
