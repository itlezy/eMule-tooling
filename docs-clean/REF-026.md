---
id: REF-026
title: Manifest — drop legacy OS entries, add Common Controls 6.0 dependency
status: Open
priority: Minor
category: refactor
labels: [manifest, win10, common-controls, baseline, dpi]
milestone: ~
created: 2026-04-09
source: stale-v0.72a-experimental-clean analysis (manifest diff)
---

## Summary

The application manifests (`srchybrid/res/emulex64.manifest`, `srchybrid/res/emuleARM64.manifest`) currently declare compatibility with Windows Vista, 7, 8, and 8.1 in addition to Windows 10. Since the project targets Win10+ only, these entries should be removed. The manifests also lack an explicit `Microsoft.Windows.Common-Controls 6.0` dependency declaration — without it, Windows injects the older v5 common controls DLL in some activation contexts, defeating the visual style theming.

## Current State (main)

```xml
<compatibility xmlns="urn:schemas-microsoft-com:compatibility.v1">
  <application>
    <!-- Windows 10 -->
    <supportedOS Id="{8e0f7a12-bfb3-4fe8-b9a5-48fd50a15a9a}"/>
    <!-- Windows 8.1 -->
    <supportedOS Id="{1f676c76-80e1-4239-95bb-83d0f6d0da78}"/>
    <!-- Windows 8 -->
    <supportedOS Id="{4a2f28e3-53b9-4441-ba9c-d69d4a4a6e38}"/>
    <!-- Windows 7 -->
    <supportedOS Id="{35138b9a-5d96-4fbd-8e2d-a2440225f93a}"/>
    <!-- Windows Vista -->
    <supportedOS Id="{e2011457-1546-43c5-a5fe-008deee3d3f0}"/>
  </application>
</compatibility>
```

No `Microsoft.Windows.Common-Controls` dependency entry.

## Target State (from experimental)

```xml
<compatibility xmlns="urn:schemas-microsoft-com:compatibility.v1">
  <application>
    <!-- Windows 10 only -->
    <supportedOS Id="{8e0f7a12-bfb3-4fe8-b9a5-48fd50a15a9a}"/>
  </application>
</compatibility>
<dependency>
  <dependentAssembly>
    <assemblyIdentity type="win32"
                      name="Microsoft.Windows.Common-Controls"
                      version="6.0.0.0"
                      processorArchitecture="amd64"
                      publicKeyToken="6595b64144ccf1df"
                      language="*"
    />
  </dependentAssembly>
</dependency>
```

(ARM64 manifest gets `processorArchitecture="arm64"`.)

## Why This Matters

1. **Removing old OS entries**: Declaring Win10 compatibility only lets the OS apply Win10-specific behavior fixes (heap, file system, timer, etc.) that require the correct `supportedOS` GUID. Listing Vista/7 suppresses some of these fixes.

2. **Adding Common Controls 6.0**: Without this entry, the activation context resolution can fall back to the version 5 comctl32.dll in certain dialog creation paths — breaking visual styles (themed buttons, list controls, tab controls). The entry was present in older eMule versions via a `<dependency>` block but was dropped at some point.

## Experimental Reference

Done in experimental in commit `8aa299d` (FIX: embed common controls dependency in manifests) and `76dd16b` (FIX: move common controls dependency into manifests), combined with the Win10-only OS targeting from `4545c65` (WIP: set Windows 10 baseline).

## Acceptance Criteria

- [ ] Both `emulex64.manifest` and `emuleARM64.manifest` list only the Windows 10 `supportedOS` GUID
- [ ] Both manifests include the `Microsoft.Windows.Common-Controls 6.0` `dependentAssembly` entry
- [ ] No regression in visual styling (buttons, list controls, tab controls all themed)
- [ ] `longPathAware` entry preserved
