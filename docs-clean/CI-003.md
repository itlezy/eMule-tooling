---
id: CI-003
title: MSVC compiler hardening — enable SDL, guard checks, and treat warnings as errors
status: In Progress
priority: Minor
category: ci
labels: [msvc, compiler, hardening, warnings]
milestone: ~
created: 2026-04-08
source: AUDIT-CODEQUALITY.md (Phase 3)
depends-on: ~
---

## Summary

Phase A hardens the app binary first with `/sdl` and `/guard:cf`, then verifies
the produced binary with `dumpbin /headers /loadconfig`. Later phases can add
warning-policy enforcement and conformance tightening.

## Current State

- `/Wall` — all warnings enabled (already set)
- `/W4` equivalent in practice (some `/Wall` warnings suppressed via `#pragma warning(disable:...)`)
- Phase A target: app project only
- Deferred: `/WX`, `/permissive-`, third-party/test-project hardening, `/analyze`

## Phase A Additions

- `srchybrid/emule.vcxproj`
  - `SDLCheck=true`
  - `ControlFlowGuard=Guard`
  - `LinkControlFlowGuard=true`
- `repos/eMule-tooling/ci/check-build-policy.ps1`
  - assert those properties for app `Debug` and `Release`
- `repos/eMule-build/workspace.ps1`
  - after `build-app`, verify `emule.exe` contains CFG metadata via `dumpbin`

## Later Phases

1. Expand hardening to tests and workspace-built dependency projects if desired.
2. Audit warning suppressions and decide whether `/WX` is realistic with `/Wall`.
3. Treat `/permissive-` as a separate conformance pass.

## Acceptance Criteria

- [ ] app `Debug` and `Release` builds pass with `/sdl`
- [ ] app `Debug` and `Release` builds pass with `/guard:cf`
- [ ] supported `build-app` verification confirms CFG metadata in the built app binary
- [ ] centralized build-policy audit fails if the app project drops those properties
- [ ] later phases remain tracked separately and are not implied complete by Phase A
