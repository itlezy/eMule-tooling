---
id: CI-011
title: Broadband release live E2E coverage umbrella
status: Done
priority: Major
category: ci
labels: [testing, live-e2e, release, rest, ui, live-wire, arr]
milestone: broadband-release
created: 2026-05-02
source: broadband release live E2E and REST completeness planning
---

## Summary

Make the broadband release live E2E lane a first-class release gate instead of
an ad hoc collection of scripts.

## Release 1.0 Classification

**Release Gate.** This owns the operator-facing first-release command and the
result artifact shape. It should aggregate the release-critical REST,
WebServer, live-wire, aMuTorrent, and Arr controller checks without making every
future UI E2E lane block `emule-bb-v1.0.0`.

The aggregate runner should cover:

- UI flows for preferences, shared files, search, and download lifecycle
- REST contract and live-wire checks
- WebServer robustness checks
- aMuTorrent integration coverage where a live eMule BB instance is required
- Prowlarr, Radarr, and Sonarr integration coverage through the live Arr suite

## Acceptance Criteria

- [x] `run_live_e2e_suite.py` has stable selectable suites for every release
      lane
- [x] each suite writes a result artifact with status, command line, timings,
      and failure phase
- [x] public network failures are reported as inconclusive with diagnostics
      when the app and harness behaved correctly
- [x] the default release lane uses isolated temp profiles and does not depend
      on user profile state
- [x] aMuTorrent, Prowlarr, Radarr, and Sonarr suites are included in the
      default release lane and produce redacted artifacts
- [x] release documentation points operators to one supported command

## Completion Evidence

- App commits: `7754d16`, `00a7da4`, `3b315a4`, `576f9a1`.
- Test commits: `13f1487`, `1058ac2`, `2bb7fd2`, `a68ac42`, `d929611`,
  `e5fca8e`, `961efac`, `1f973ae`, `e952b77`.
- Tooling commits: `cb0fa27`, `eb079f8`, `bab2ac8`, `56bb1bb`.
- Release x64 `workspace.ps1 validate`, Debug/Release app builds,
  Debug/Release test builds, focused REST live smoke, REST live soak,
  preference UI live E2E, and full umbrella live E2E passed.
- Full umbrella artifact:
  `repos\eMule-build-tests\reports\live-e2e-suite\20260506-224844-eMule-main-release\result.json`.
  The run passed with `auto-browse-live` inconclusive only because no
  browse-capable live source was available.
- Clean-worktree audit passed with
  `pwsh -File repos\eMule-tooling\ci\check-clean-worktree.ps1 -EmuleWorkspaceRoot .`.
  Release identity, release-note product naming, and ZIP asset naming were
  reviewed against `docs\WORKSPACE_POLICY.md`, `docs-clean\RELEASE-1.0.md`,
  and `docs-clean\RELEASE-1.0-RUNBOOK.md`.

## Relationship To Other Items

- owns the umbrella for `CI-012` through `CI-016`
- owns the aggregate release lane for `AMUT-001` and `ARR-001`
- complements `CI-008`, which remains the older broad regression umbrella
